
public class UnsortedDateList extends DateList{
   
   public UnsortedDateList() {
      // uses the constructor from the super class
      super();
   }
   
   public void add(Date212 d) {
      // will add onto the end of the DateList
      append(d);
   }
   
}
